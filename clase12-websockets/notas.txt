1. 
. crear server.js
. npm init -y
. npm i express socket.io


. 
. importar express
. importar httpServer de http (destructuring y renombrando { Server : HttpServer})
. importar server de socket.io (destructuring y renombrando { Server : IOServer})

. const app = express()
. instancio http = new HttpServer(app)
. instancio express

3.
httpServer.listen(puerto, () => console.log('etc etc))

4. app.get('/',, (req, res) => {
    console.log('hola servidor)
})

//ya levanta el servidor con esto

//consigna
. hay que instalar FETCH, no es nativo de node